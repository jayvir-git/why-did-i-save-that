{
  "name": "why-did-i-like-or-save-that-inator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.cjs && node scripts/build.cjs --check"
  }
}
